on:
  schedule:
    - cron: '0 */6 * * *' # 6시간마다 실행 (UTC 기준 0, 6, 12, 18시)
  workflow_dispatch: # 수동 실행 트리거

permissions:
  contents: write

jobs:
  run-collector:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout main branch
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          python -m playwright install chromium

      - name: Run data collection script
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python src/get_cpaad_wedding_api.py
          
      - name: Remove captures before deployment
        run: rm -rf data/captures

      - name: Deploy data back to api-data branch
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./data
          publish_branch: api-data
          force_orphan: false
          commit_message: "Update wedding data [skip ci]"
          user_name: 'github-actions[bot]'
          user_email: 'github-actions[bot]@users.noreply.github.com'
